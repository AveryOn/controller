
/**
 * Имена всех переменных которые используются в LocalStorage 
 */
export enum LocalVars {
    userData = 'user-data',
    token = 'token',
    currentRoute = 'current_route',
    materialsFullLabel = 'materials-full-label',
}